az --version

# azure login
az login

# set azure subscription
az account list

# create resource group
az group create --name caRG --location eastus

# Create a deployment at resource group from a local template file, using parameters from an array string.
az deployment group create --resource-group caRG --template-file main.bicep --mode Incremental --verbose